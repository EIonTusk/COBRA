node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
playwright-report

# Tauri
/src-tauri/target
/src-tauri/gen
/src-tauri/Cargo.lock
/src-tauri/icons

# Local scratchpad
TODO.md
CLAUDE.md
/plans

# Local-only secrets provisioning script (generates the Android release keystore)
scripts/setup-android-signing.sh

# Android signing keystores — never commit private signing material
*.keystore
*.jks

# Stockfish assets (fetched by scripts/prep-stockfish.mjs, too large for git)
static/stockfish/*.wasm
static/stockfish/*.js
static/stockfish/*.nnue
static/stockfish/*.d.ts
!static/stockfish/.gitkeep
